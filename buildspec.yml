version: 0.2

phases:
  install:
    commands:
      - npm i npm@latest -g
      - pip install --upgrade pip
      - pip install awscli
      - aws s3 ls
  pre_build:
    commands:
      - ls
  build:
    commands:
      - ls
  post_build:
    commands:
      - aws s3 ls